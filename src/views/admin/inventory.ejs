<%- include ("../partials/header") %>

<div>
    <h2>Items Index</h2>
    <% items.forEach(function (item, i) { %>
        <p> <%= item.id %> </p>
        <p> <%= item.name %> </p>
    <% }) %>
</div>
